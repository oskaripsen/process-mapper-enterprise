# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (SQLite - file-based, no installation needed!)
# Path to SQLite database file (relative to backend directory or absolute path)
DATABASE_PATH=process_mapper.db

# JWT Authentication
JWT_SECRET_KEY=change-this-to-a-secure-random-string

# Server Configuration
PORT=8000

# CORS Configuration (optional, comma-separated additional origins)
ALLOWED_ORIGINS=
